import React from 'react';

export default class EmployeeInfo extends React.Component {
    render(){
        return(
            <div>
            <h1>{this.value.length}</h1>
            </div>
        )
    }
}